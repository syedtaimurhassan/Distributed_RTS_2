# Normalized Case Schema

Normalized case exports are intended to capture:

- canonicalized identifiers
- resolved topology entities
- stream definitions with derived frame metadata
- route definitions mapped to canonical IDs
- optional expected WCRT baselines

The exported schema is JSON-based for now and may evolve as the assignment scope is refined.
