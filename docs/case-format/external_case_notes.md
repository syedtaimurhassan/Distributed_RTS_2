# External Case Notes

External case files are adapter inputs, not internal domain objects. The scaffold assumes a manifest plus separate topology, routes, streams, and optional expected WCRT files, but the adapter layer is where format-specific handling belongs.
