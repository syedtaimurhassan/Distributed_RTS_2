# Output Contracts

The project reserves separate artifact spaces for:

- normalized canonical case exports
- raw subsystem JSON results
- detailed CSV traces
- derived summary tables
- run metadata and artifact indices

Exact row schemas remain iterative, but the file ownership is already separated by subsystem.
