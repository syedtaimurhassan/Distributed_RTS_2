# Testing

Current tests focus on scaffold correctness:

- importability
- CLI parser wiring
- run layout creation
- case loading, validation, and normalization smoke coverage

As implementation deepens, add unit coverage per subsystem and golden-output regression tests.
