# Normalized Cases

This directory is reserved for exported canonical case representations generated by the normalization pipeline.

Each normalized export bundle includes:

- `normalized_case.json`
- `nodes.csv`
- `links.csv`
- `routes.csv`
- `streams.csv`
- `link_stream_map.csv`
- `artifact_index.json`
